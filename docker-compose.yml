version: '3.8'

services:
  web:
    build: ./docker/apache
    container_name: iubmdb_web
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
    restart: always
    working_dir: /var/www/html
